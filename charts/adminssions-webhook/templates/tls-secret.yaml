apiVersion: v1
kind: Secret
metadata:
  name: {{ include "admissions-webhook.fullname" . }}-tls
  namespace: {{ .Release.Namespace }}  
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.tls.crt }}
  tls.key: {{ .Values.tls.key }}
