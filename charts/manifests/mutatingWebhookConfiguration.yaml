apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: pod-admissions-webhook-mutate
webhooks:
  - name: mutate.admissions-webhook.lukaross368.io
    clientConfig:
      service:
        name: webhook
        namespace: admissions-webhook
        path: /mutate-pods
        port: 443
      caBundle: <add base64 encoded cert>
    rules:
      - operations: ["CREATE", "UPDATE"]
        apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["pods"]
        scope: "Namespaced"
    admissionReviewVersions: ["v1"]
    sideEffects: None
    timeoutSeconds: 10
    failurePolicy: Fail
